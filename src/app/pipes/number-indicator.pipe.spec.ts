import { NumberIndicatorPipe } from './number-indicator.pipe';

describe('NumberIndicatorPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberIndicatorPipe();
    expect(pipe).toBeTruthy();
  });
});
